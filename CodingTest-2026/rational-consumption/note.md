# Problem: Rational Consumption
 
문제 요약: 가장 비싼 물품과 가장 저렴한 물품 구하는 문제

## 풀이

### 문제 정보
- 난이도: Lv 00 
- 주제: 키워드
- 문제 핵심 
  - 예: 정렬 후 이분 탐색
  - 예: 양쪽 후보 비교 
- 최초 풀이: 260811 
- 수정 버전: 260811 

### 접근법
1. Solution1: 핵심 풀이 방식 (권장)
   - 설명 (왜 이 방식으로 푸는지 한 줄 설명)
2. Solution2: 방식 (대안 풀이 방식들 - 여러 개 있으면 숫자 추가 구성, 비효율적이면 적지 X)
   - 설명 (왜 이 방식으로 푸는지 한 줄 설명)
   - 한계: 가능은 하지만 권장도가 낮은 이유 또는 특징 설명


## Solution1: 방식 (권장)
### 코드 설계
1. 실제 코드의 첫 번째 동작
2. 초기화할 변수
3. 반복문 안에서 할 일
   - 조건에 따른 갱신
   - 누적
   - 비교
4. 반환 형식

### Java
- 구현 파일: `Solution1.java`
- 핵심 문법:
  - 예: `BufferedReader`
  - 예: `StringTokenizer`
  - 예: `Math.min()`, `Math.max()`

### Python
- 구현 파일: `Solution1.py`
- 핵심 문법:
  - 예: `input()`
  - 예: `map()`
  - 예: `min()`, `max()`

### 복잡도
- 시간복잡도: 
- 공간복잡도: 


## Solution2: 방식
### 코드 설계
1. 실제 코드의 첫 번째 동작
2. 초기화할 변수
3. 반복문 안에서 할 일
   - 조건에 따른 갱신
   - 누적
   - 비교
4. 반환 형식

### Java
- 구현 파일: `Solution1.java`
- 핵심 문법:
  - 예: `BufferedReader`
  - 예: `StringTokenizer`
  - 예: `Math.min()`, `Math.max()`

### Python
- 구현 파일: `Solution1.py`
- 핵심 문법:
  - 예: `input()`
  - 예: `map()`
  - 예: `min()`, `max()`

### 복잡도
- 시간복잡도: 
- 공간복잡도: 

